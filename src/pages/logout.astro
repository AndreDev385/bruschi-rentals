---
import { getLogoutUrl } from "@/lib/auth";

// Clear the session cookie
Astro.cookies.set("auth-session", "", {
  maxAge: 0,
  httpOnly: true,
  secure: import.meta.env.PROD,
  sameSite: "strict",
  path: "/",
});

return Astro.redirect(getLogoutUrl());
---
