---
export const prerender = false;

import { Image } from "astro:assets";
import Layout from "@/layouts/Layout.astro";
import { getSession } from "@/lib/auth";
import Hero from "@/assets/hero.jpg";

const session = await getSession(Astro);

if (!session) {
  return Astro.redirect("/login");
}

// Set welcome seen cookie server-side
Astro.cookies.set("welcome_seen", "true", {
  path: "/",
  httpOnly: false, // Allow client access if needed
  secure: import.meta.env.PROD,
  sameSite: "lax",
});

const url = new URL(Astro.request.url);
const search = url.search;
---

<Layout>
  <section class="relative w-full min-h-screen flex items-center py-20">
    <div class="absolute inset-0 z-0">
      <div
        class="absolute inset-0 bg-gradient-to-b from-black/70 via-obsidian/70 to-black/70"
      >
      </div>
      <Image
        src={Hero}
        alt="Hero"
        loading="eager"
        class="w-full h-full object-cover"
      />
    </div>

    <div class="container mx-auto px-4 sm:px-6 lg:px-8 relative z-20">
      <div class="max-w-2xl mx-auto text-center">
        <div id="confetti-container" class="mb-8"></div>

        <div class="bg-white rounded-xl shadow-lg p-8 mb-8">
          <h1 class="text-4xl font-bold text-obsidian mb-4">
            Welcome to Bruschi Rentals! 🎉
          </h1>
          <p class="text-xl text-mocha mb-6">
            Inside this portal, you’ll start seeing options that match what you
            shared with us - so you can explore at your own pace and feel
            confident about your next step.
          </p>

          <div class="space-y-4">
            <p class="text-lg text-obsidian">
              Your realtor will upload curated options here. You’ll be able to
              save your favorites, leave notes about what you like or don’t
              like, request tours, and schedule showings easily.
            </p>

            <div class="bg-soft-light p-6 rounded-lg">
              <h3 class="text-lg font-semibold text-primary mb-2">
                What to expect:
              </h3>
              <ul class="text-left text-mocha space-y-1">
                <li>• Options selected based on your goals</li>
                <li>• Clear photos & detailed info</li>
                <li>• Favorite, comment & request tours</li>
                <li>
                  • Direct communication with Paulina & the Bruschi Rentals team
                </li>
              </ul>
            </div>

            <h5 class="text-lg font-semibold text-primary">
              You’re one step closer to your next home.
            </h5>
          </div>
        </div>

        <a
          href={`/portal${search}`}
          class="inline-block px-8 py-4 bg-primary text-white font-bold rounded-lg hover:bg-primary/90 transition-all duration-300 transform hover:scale-105"
        >
          Enter My Portal
        </a>
      </div>
    </div>
  </section>
</Layout>

<script>
  import confetti from "canvas-confetti";

  // Fire confetti on page load
  confetti({
    particleCount: 100,
    spread: 70,
    origin: { y: 0.6 },
  });

  // Additional bursts for celebration
  setTimeout(() => {
    confetti({
      particleCount: 50,
      angle: 60,
      spread: 55,
      origin: { x: 0 },
    });
    confetti({
      particleCount: 50,
      angle: 120,
      spread: 55,
      origin: { x: 1 },
    });
  }, 500);
</script>
